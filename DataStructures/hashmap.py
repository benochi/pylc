# insert remove search 3 main functions for DSs.
# treemap = ologn for all 3
# hashmap - insert o(1), removing o(1), searching o(1)
# downside, no ordering for hashmaps, O(n), would have to sort, n log n
